defmodule MixTest.Tasks.Workshop.Check do
  use ExUnit.Case

  @tag :pending
  test "pick the first module name in cases where multiple modules are defined in the check file" do
  end

  @tag :pending
  test "should return true if there is no prerequisite file" do
  end

  @tag :pending
  test "should return false if the prerequisite test fails" do
  end
end
